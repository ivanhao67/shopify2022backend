html
  head
    title #{item.name}
body
  h1 Editing #{item.name}
  form(action = `${item.id}` method = 'POST')
    input(type = 'text' name = 'itemname' value=`${item.name}` required)
    br
    input(type = 'text' name = 'qty' value=`${item.stock}` required)
    br
    button(type = 'submit' name='id' value=`${item.id}`) update



			
